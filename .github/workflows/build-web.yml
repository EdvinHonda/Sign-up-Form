name: Build web of sign-in form
run-name: Deploy
on:
  push:
    branches:
      - main
    tags:
        - v1.*
  pull_request: 
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: build
        run: echo 'building'

  publish:
    needs: [build]
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: publish
        run: echo 'publishing'
